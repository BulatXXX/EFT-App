<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EFT_APP" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="textFillColor">@color/black</item>

        <!-- Secondary brand color. -->
        <!-- Status bar color. -->
        <item name="backgroundColor">@color/sand_color</item>
        <item name="android:statusBarColor">@color/sand_color</item>
        <item name="android:navigationBarColor">@color/sand_color</item>

        <!-- Customize your theme here. -->
    </style>
</resources>