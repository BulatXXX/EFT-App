<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EFT_APP" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="textFillColor">@color/white</item>
        <!-- Secondary brand color. -->
        <!-- Status bar color. -->
        <item name="backgroundColor">@color/dark_violet</item>
        <item name="android:statusBarColor">@color/dark_violet</item>
        <item name="android:navigationBarColor">@color/dark_violet</item>
        <!-- Customize your theme here. -->
    </style>
</resources>